# === Secrets — NEVER commit these ===
.env
.env.*
!.env.example
*.pem
*.key

# === Private strategy docs — keep OUT of the repo entirely (never public) ===
docs/PRD.md

# === Real personal data — the committed resume.json/cases.json are FICTIONAL
# template data; the .private.json twins hold the real resume and eval cases.
# The deploy script and eval harness prefer the private files when present. ===
data/resume.private.json
evals/cases.private.json
# The function's bundled copy is generated by scripts/deploy-backend.sh from
# whichever resume is active — never commit it, it may contain the real one.
supabase/functions/vita-handler/resume.json

# === Claude Code local config (machine-specific paths; keep private) ===
.claude/settings.local.json
.claude/settings.json

# === Dependencies ===
node_modules/
.pnp
.pnp.js

# === Build output ===
dist/
build/
.next/
out/
*.tsbuildinfo

# === Vercel ===
.vercel/

# === Supabase (local dev artifacts) ===
supabase/.temp/
supabase/.branches/

# === Editor / OS cruft ===
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*

# === Eval reports are generated from live answers about the REAL resume —
# never commit them (contact info is auto-redacted, but the answers
# themselves contain real personal facts) ===
evals/reports/
__pycache__/
