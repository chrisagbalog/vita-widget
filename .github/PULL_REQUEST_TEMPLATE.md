## What & why

<!-- One or two sentences: what changes, and what problem it solves. -->

## Checks

- [ ] `npm test` passes
- [ ] `npm run build` passes (includes the TypeScript check)
- [ ] **If this touches the prompt, `resume.json`, or the backend:** full eval
      run passes the gate (`python3 evals/run.py`, ≥49/51) — paste the result line below
- [ ] No new runtime dependencies (or the "why" is explained above)

<!-- Eval result line (if applicable):
RESULT: NN/51 passed
-->
